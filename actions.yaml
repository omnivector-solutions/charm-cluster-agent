clear-cache-dir:
  description: >
    Clear cluster-agent's cache directory.
upgrade:
  description: >
    Upgrade cluster-agent.
  params:
    version:
      type: string
      description: Version of cluster-agent to upgrade to.
  required:
    - version
