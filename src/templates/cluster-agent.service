[Unit]
Description=cluster-agent
After=network.target

[Service]
Type=oneshot
User=root
Group=root
WorkingDirectory=/srv/cluster-agent-venv
ExecStart=/srv/cluster-agent-venv/bin/agentrun

[Install]
Alias=cluster-agent.service
WantedBy=multi-user.target
